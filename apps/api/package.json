{
  "name": "@mckoy/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply mckoy_db --local",
    "migrate:remote": "wrangler d1 migrations apply mckoy_db --remote",
    "seed:admin": "node scripts/seed-admin.mjs",
    "revoke:admin": "node scripts/revoke-admin.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@simplewebauthn/server": "^13.1.1",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.6.4",
    "@cloudflare/workers-types": "^4.20241205.0",
    "@types/node": "^22.10.5",
    "typescript": "^5.7.2",
    "vitest": "~2.1.8",
    "wrangler": "^4.0.0"
  }
}
